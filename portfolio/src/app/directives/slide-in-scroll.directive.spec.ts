import { SlideInScrollDirective } from './slide-in-scroll.directive';

describe('SlideInScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInScrollDirective();
    expect(directive).toBeTruthy();
  });
});
